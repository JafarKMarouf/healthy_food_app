class AppDuration {
  static const Duration splashDuration = Duration(milliseconds: 1500);
  static const Duration transitionDuration = Duration(milliseconds: 400);
  static const Duration btmSheetDuration = Duration(milliseconds: 1000);
  static const Duration dialogDuration = Duration(milliseconds: 2000);
  static const Duration showDialog = Duration(milliseconds: 1200);
}
