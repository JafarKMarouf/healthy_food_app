class AppDuration {
  static const Duration splashDuration = Duration(milliseconds: 800);
  static const Duration transitionDuration = Duration(milliseconds: 400);
  static const Duration btmSheetDuration = Duration(milliseconds: 1000);
  static const Duration dialogDuration = Duration(milliseconds: 3000);
  static const Duration showDialog = Duration(milliseconds: 2000);
  static const Duration showWarning = Duration(milliseconds: 700);
  static const Duration otpDuration = Duration(milliseconds: 200);
}
